# Fórmula para a área de um triangulo: base * altura

print("Entre com os dados do triângulo: ")
base = float(input("Base: "))
height = float(input("Altura: "))

area = base * height

print("A área do triângulo é igual: ", area)
